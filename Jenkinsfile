pipeline {
    agent any
    stages {
        stage('git commit') {
            steps {
                echo 'welcome'
                echo 'wlcome to pipeline cloud' >> file.txt
                  }
             }
    }
}
