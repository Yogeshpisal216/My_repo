pipeline {
    agent any
    stages {
        stage('git commit') {
            steps {
                bash '''
                echo 'welcome'
                echo 'wlcome to pipeline cloud' >> file.txt
                '''
                  }
             }
    }
}
