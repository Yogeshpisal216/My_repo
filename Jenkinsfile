pipeline {
    agent any
    stages {
        stage('git commit') {
            steps {
                echo 'welcome'
                git 'https://github.com/Yogeshpisal216/student.git'
                  }
             }
    }
}
